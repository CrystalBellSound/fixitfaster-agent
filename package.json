{
  "name": "fixitfaster-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Datadog Agent + demo containers for Fix It Faster hands-on",
  "scripts": {
    "up": "docker compose --env-file .env.local up -d",
    "down": "docker compose --env-file .env.local down",
    "logs": "docker compose --env-file .env.local logs -f agent",
    "pipeline:setup": "bash scripts/setup-log-pipeline.sh",
    "pipeline:setup:py": "python3 scripts/setup-log-pipeline.py",
    "up:full": "npm run up && npm run pipeline:setup"
  }
}
